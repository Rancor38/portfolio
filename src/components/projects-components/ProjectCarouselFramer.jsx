import React from 'react';
import { motion, AnimatePresence } from "framer-motion"

const ProjectCarouselFramer = ({ image }) => {
    return (
        <div>
            
        </div>
    );
};

export default ProjectCarouselFramer;